2019.10.5
1、完成简单发布新帖和发布评论的功能。
2、对于用户信息统计，不会显示最近一次操作是1970年了。
3、对于评论的帖子，在主题也不再显示最后一次评论是1970年了。
4、开发重新编辑评论的功能。完成评论编辑提交后，可以直接回到对应的帖子评论页，但是实现方式需要带着cmtIndex走两个页面，感觉比较乱。
5、开发重新编辑帖子标题的功能。

2019.10.4
1、赞踩时加上验证。
2、用户信息统计的评论数，是真正的评论数。帖子的评论数，也是真正的评论数了。
3、修改了新增帖子的逻辑，在同一个事务中，同时完成新增帖子和主楼评论。
4、做了一部分发表新帖的功能。

2019.10.2-3
1、经过很长时间研究，最终仍旧没能找到可执行多个以";"分割的多个sql语句的方法。
2、修改mysql数据库表的命名，表统一加上tb_前缀，字段统一加上对应表简写的前缀，如：ur_、po_、cmt_等
3、完成dba的逻辑更新（加入新的计数器字段后）和测试。
4、完成基准测试，主要的查询过程非常快，而写入很慢。修改部分mysql参数配置。
    插入一条评论，并更新所有计数器：7ms
    批量方式每插入一条评论,并更新所有计数器：1.0ms
    随机赞踩，并更新所有计数器：5.0ms
    按照发帖顺序，查找主题页帖子(20个)：0.7ms
    按照最后评论顺序，查找主题页帖子(20个)：0.8ms
    查看帖子评论内容(一页20个评论)：0.8ms

2019.10.1
1、更新dba代码，适应修改后的数据库表结构。并完成测试。

2019.9.28
1、采用MD5的方式来处理密码。
2、修改部分dba的sql语句和调用方式，兼容mysql
3、数据库表结构优化：
    a、user表，新建索引：ID、name，形成更多覆盖索引，因为表内都存了userID，而没有存userName
    b、theme表新增计数器字段：
        1、postCount uint 主题内帖子总数量
    c、主题表，新建索引：ID，name，形成更多覆盖索引。
    d、user表新增计数器字段：
        1、postCount uint 用户发帖总量
        2、cmtCount uint 用户发表评论总量（含发帖量）
        3、praiseTimes uint 用户被赞总量
        4、belittleTimes uint 用户被踩总量
        5、lastEditTime int64 最后编辑（发帖、改帖、发评论、改评论）时间。
        6、lastEditID uint64 最后编辑的内容ID
    e、post表新增计数器字段：
        1、postTime int64 发帖时间
        2、cmtCount uint 帖子评论总量（含主贴）
        3、lastCmterID uint 最终评论人ID
        4、lastCmtTime int64 最终评论时间
4、需要新增赞踩记录表。下次再考虑。
