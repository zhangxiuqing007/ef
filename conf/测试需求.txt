未完成单元测试：
package：dba
	AddImages(images []*models.ImageInDB) error                      		//批量新增图片  写完，未测试。
	QueryImages(userID int, count int, offset int) ([]string, error) 		//图片查询  写完，未测试。
	SetPB(cmtID int, userID int, isP bool, isD bool) error    		 //尝试设置PB   写完，未测试。
	UpdateUserHeadPhoto(userID int, path string) error                                  //更新头像文件路径 写完，未测试。
	QueryImageCountOfUser(userID int) (int, error)                                      //查询用户上传图片数量 写完，未测试。

